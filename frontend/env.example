# Configuration des variables d'environnement pour le frontend
# Copier ce fichier en .env.local pour le développement local

# URL de l'API backend (production)
# En développement, le proxy Vite (/api) redirige automatiquement vers le backend
# En production, cette variable doit être définie sur Render
VITE_API_URL=https://kairos-0aoy.onrender.com/api

# Note: Si VITE_API_URL n'est pas définie, le code utilisera par défaut:
# - En dev: /api (proxy Vite)
# - En prod: https://kairos-0aoy.onrender.com/api
